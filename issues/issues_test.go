package issues_test

import (
	. "github.com/onsi/ginkgo"
	. "github.com/onsi/gomega"
)

const issuesStub = `

`

var _ = Describe("building issues from JSON", func() {
	It("builds a slice of issues from raw JSON", func() {

		By("filtering out pull requests")

	})
})
